apiVersion: v1
kind: Service
metadata:
  name: hostly-web-service
  labels:
    app: hostly-web
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: hostly-web

