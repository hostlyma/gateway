apiVersion: v1
kind: Secret
metadata:
  name: pgadmin-secret
  labels:
    app: pgadmin
type: Opaque
stringData:
  # pgAdmin login credentials - CHANGE THESE!
  PGADMIN_EMAIL: "admin@hostly.com"  # Your pgAdmin login email
  PGADMIN_PASSWORD: "ChangeMe123!"    # Your pgAdmin login password (use a strong password!)

