apiVersion: v1
kind: Service
metadata:
  name: pgadmin-service
  labels:
    app: pgadmin
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30433  # External port for pgAdmin
    protocol: TCP
    name: http
  selector:
    app: pgadmin

